package starbuzz.interfaces;

/**	Recipe contains the instructions for preparing a
 *		particular beverage. */
public interface Recipe {
	/**	Returns a line-by-line delineation of how to prepare
	 *		a beverage recipe. */
	public String prepare();
}
